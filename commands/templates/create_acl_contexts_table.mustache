'use strict'

const Schema = use('Schema')

class AclContextsTableSchema extends Schema {
  up () {
    this.create('acl_contexts', table => {
      table.increments()
      table.string('slug').notNullable().unique()
      table.string('name').notNullable().unique()
      table.text('description').nullable()
      table.timestamps()
    })
  }

  down () {
    this.drop('acl_contexts')
  }
}

module.exports = AclContextsTableSchema
